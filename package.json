{
  "name": "nine-lives-cat-camera",
  "version": "0.2.0",
  "private": true,
  "description": "Raspberry Pi camera stream with continuous cat inference",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "setup:python": "python3 -m venv --clear .venv && .venv/bin/python -m pip install -r requirements.txt",
    "check": "npm run check:node && npm run check:python",
    "check:node": "node --check server.mjs && node --check src/camera-publisher.mjs && node --check src/detection-log.mjs && node --check src/event-clip-recorder.mjs && node --check src/feeding-window-log.mjs && node --check src/inference-publisher.mjs && node --check src/inference-schedule.mjs && node --check src/inference-worker.mjs && node --check public/app.js && node --check public/schedule.js && node --check tests/fixtures/mock-inference-worker.mjs",
    "check:python": "python3 -c \"import ast,pathlib; ast.parse(pathlib.Path('inference_stream.py').read_text())\"",
    "test": "node --test"
  },
  "type": "module"
}
